apiVersion: v2
name: litellm
description: LiteLLM proxy + version-controlled PostgreSQL
type: application
version: 0.2.0
appVersion: "1.88.0"

dependencies:
  - name: litellm-helm
    version: "1.88.0"
    condition: litellm-helm.enabled
  - name: postgresql
    version: 0.2.0
    condition: postgresql.enabled
