name: Release Charts

on:
  push:
    branches:
      - main

jobs:
  release:
    permissions:
      contents: write
      packages: write
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run chart-releaser
        uses: helm/chart-releaser-action@v1.6.0
        with:
          config: .cr.yaml
        env:
          CR_TOKEN: "${{ secrets.GITHUB_TOKEN }}"

      - name: Publish to GitHub Pages
        uses: helm/chart-releaser-pages@v1.6.0
        with:
          release_name_template: __SKIP__
        env:
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
